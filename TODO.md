# TODO - Parallel Wiring Harness page updates

- [ ] Inspect existing `parallel-wiring-harness.html` product grids/tiles.
- [ ] Inspect image handling in `index.html` products section (how images are embedded and styled).
- [ ] Add consistent product image markup to every product tile on `parallel-wiring-harness.html` (placeholders for image paths).
- [ ] Ensure images render only inside the designated image area (no spill into tile) and remain fully visible (no cropping) using appropriate CSS (`object-fit: contain` with fixed image viewport).
- [ ] Update Machine Room harness section:
  - [ ] Add one more tile: `Motor fan cable`.
- [ ] Update Field harness section:
  - [ ] Remove tiles: `Round travelling cable` and `Flat travelling cable (12 core)`.
  - [ ] Add tiles: `Connector Base Travelling cable` and `Display & call harness extension`.
- [ ] Update Cartop section:
  - [ ] Remove tiles: `Retiring cam` and `Display to button`.
  - [ ] Add tiles: `Ram Striker Harness` and `Parallel Harness`.
- [ ] Verify styling consistency with existing theme (`style.css`).
- [ ] Run a quick local check (open HTML in browser) to confirm layout and no image cutting.

